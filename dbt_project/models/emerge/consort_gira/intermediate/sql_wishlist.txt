sql requests

gender vs condition male(201617,198198, 197237 ,198197,  197039 ,4150042,440971 ,201257,200052 ,199078   )

dups in procedure_occurrence_id, condition_id, observation_id

Done: 
- Move the year_of_birth calculation into an intermediate person table (person_person) so I can just use birth_date in my domain_occurence_date field. NULL yob = yob 1970

- For care_site, we need to extract the unique two-digit combinations from all emerge ids (presumably in the Person table). From there we need to be able to map those two digit codes to site enumerations (e.g., 9512345 should be 95 and then that prefix maps to CHOP)
  - Added dbt_project/seeds/care_site_seeds
  - Added models/emerge/consort_gira/lookups/emerge_consort_gira_lookup_care_sites
- For the visit table, we need all encounter IDs from all tables (CPT, ICD, BMI, and Measurement) grouped by participant (e.g., Participant 9512345 has encounter IDs 1, 2, 3, and 4) and then we need to make a list of all ID/encounter_id combinations (the example participant creates 4 rows in visit)


-  primary key creation. Move back to int stage?
-  Figure out what to do about int_measurement_measurement memory.
-  If demographics ids are unknown make sure Standards are set to 0
-  BMI needs to ignore Standard mapping from CR join.